extends commonLayout

block content

  div(data-role="controlgroup", data-theme='b')
    select
      -for (var i=0; i < slides.length; i++)
        -var slide = slides[i];
        option(value="#{slide._id}", label="#{slide.name}")=slide.name

